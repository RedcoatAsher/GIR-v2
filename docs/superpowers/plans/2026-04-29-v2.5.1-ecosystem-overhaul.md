# GIR v2.5.1 Ecosystem Overhaul Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename core plugin to `gir-core`, fix marketplace display name, resolve sequential-thinking MCP conflict, add gir-ai integration prompt, rename gir-tools to gir-atc, and cut v2.5.1 release.

**Architecture:** Six independent tasks executed sequentially — Tasks 1–5 are naming/structural changes with no shared state conflicts; Task 6 (version bump) runs last after all renames are stable. Each task commits independently. The core rename (Task 2) is the highest blast-radius change: 7 spoke plugins carry `requires: ["gir"]` that must all flip to `gir-core`.

**Tech Stack:** JSON manifests, Markdown files, git, GitHub CLI (`gh`)

---

## File Map

**Modified across Tasks 1–2 (core rename + marketplace fix):**
- `.claude-plugin/marketplace.json` — collection name + gir entry name/source
- `plugins/gir/.claude-plugin/plugin.json` → becomes `plugins/gir-core/`
- `plugins/gir/gir-module.json` → `plugins/gir-core/gir-module.json`
- `plugins/gir/hooks/hooks.json` → `plugins/gir-core/hooks/hooks.json`
- `plugins/gir-web/gir-module.json`, `plugins/gir-automation/gir-module.json`, `plugins/gir-tools/gir-module.json`, `plugins/gir-database/gir-module.json`, `plugins/gir-ai/gir-module.json`, `plugins/gir-qa/gir-module.json`, `plugins/gir-migrate/gir-module.json` — `requires` field

**Modified in Task 3 (sequential-thinking fix):**
- `plugins/gir-core/.mcp.json` — server key rename
- `plugins/gir-core/gir-module.json` — MCP entry key
- `plugins/gir-core/skills/routing-stub.md` — MCP server name reference

**Modified in Task 4 (gir-ai install flow):**
- `plugins/gir-ai/hooks/hooks.json` — add integration detection prompt
- `plugins/gir-ai/templates/ai-integrations.md` — new template file

**Modified in Task 5 (gir-tools → gir-atc):**
- `plugins/gir-tools/` directory → `plugins/gir-atc/`
- `plugins/gir-atc/.claude-plugin/plugin.json`
- `plugins/gir-atc/gir-module.json`
- `plugins/gir-atc/hooks/hooks.json`
- `plugins/gir-atc/README.md`
- `.claude-plugin/marketplace.json` — gir-tools entry → gir-atc
- `README.md` — all gir-tools references

**Modified in Task 6 (version bump v2.5.1):**
- All `plugin.json`, `gir-module.json`, `hooks.json` version strings (25 files)
- `.claude-plugin/marketplace.json` metadata.version
- `README.md` badge

---

## Task 1: Fix Marketplace Display Name (`gir-plugins` → `gir`)

The marketplace collection is named `"gir-plugins"` which surfaces as "gir-plugin" in the Claude Code TUI. Rename it to `"gir"`.

**Files:**
- Modify: `.claude-plugin/marketplace.json:2`

- [ ] **Step 1: Update marketplace.json collection name**

In `.claude-plugin/marketplace.json`, change line 2:
```json
"name": "gir-plugins",
```
to:
```json
"name": "gir",
```

- [ ] **Step 2: Commit**

```bash
git add .claude-plugin/marketplace.json
git commit -m "fix: rename marketplace collection from gir-plugins to gir"
```

---

## Task 2: Rename Core Plugin `gir` → `gir-core`

The core plugin directory and all name references change from `gir` to `gir-core`. All seven spoke plugins carry `requires: ["gir"]` — these must all update to `gir-core`. The marketplace.json source path updates too.

**Files:**
- Rename dir: `plugins/gir/` → `plugins/gir-core/`
- Modify: `plugins/gir-core/.claude-plugin/plugin.json`
- Modify: `plugins/gir-core/gir-module.json`
- Modify: `plugins/gir-core/hooks/hooks.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `plugins/gir-web/gir-module.json`
- Modify: `plugins/gir-automation/gir-module.json`
- Modify: `plugins/gir-tools/gir-module.json`
- Modify: `plugins/gir-database/gir-module.json`
- Modify: `plugins/gir-ai/gir-module.json`
- Modify: `plugins/gir-qa/gir-module.json`
- Modify: `plugins/gir-migrate/gir-module.json`

- [ ] **Step 1: Rename directory**

```bash
git mv plugins/gir plugins/gir-core
```

- [ ] **Step 2: Update core plugin.json**

In `plugins/gir-core/.claude-plugin/plugin.json`, change `"name": "gir"` to `"name": "gir-core"`:
```json
{
  "name": "gir-core",
  "version": "2.2.0",
  "description": "Core Claude Code productivity system — agents, routing-stub orchestration, workflows, and best practices",
  "author": { "name": "rivit-studio", "url": "https://github.com/rivit-studio" },
  "homepage": "https://github.com/rivit-studio/GIR",
  "repository": "https://github.com/rivit-studio/GIR",
  "license": "MIT",
  "keywords": ["productivity", "agents", "workflows", "best-practices", "delegation"]
}
```

- [ ] **Step 3: Update core gir-module.json**

In `plugins/gir-core/gir-module.json`, change `"name": "gir"` to `"name": "gir-core"`:
```json
{
  "name": "gir-core",
  "version": "2.2.0",
  "type": "core",
  "description": "Core agents, workflows, and best practices",
  "requires": [],
  "provides": {
    "agents": [
      "feature-architect",
      "code-reviewer",
      "debugger",
      "team-lead",
      "spec-analyst",
      "parallel-orchestrator",
      "autonomous-executor"
    ],
    "skills": [
      "routing-stub",
      "core-practices",
      "state-machines"
    ],
    "skills_on_demand": [
      "workflows",
      "ralph-loops",
      "specgates",
      "parallel-agents",
      "autonomous-mode"
    ],
    "commands": [
      "init-project",
      "init-memory-bank",
      "drift-check",
      "status",
      "modules",
      "load-workflows",
      "load-specgates",
      "load-ralph",
      "update",
      "init-setup",
      "parallel",
      "run"
    ],
    "mcp": {
      "gir-sequential-thinking": {
        "description": "Extended reasoning for complex planning and debugging",
        "commands": ["sequentialthinking"]
      }
    }
  }
}
```

Note: The MCP key is already updated to `gir-sequential-thinking` here to match Task 3 — apply both changes together.

- [ ] **Step 4: Update core hooks.json**

In `plugins/gir-core/hooks/hooks.json`, change all three occurrences of `gir` to `gir-core` in the prompt text. The full updated file:

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Silently run the following on session start:\n\n1. If .gir/MISSION.md exists, read it to load active priorities and unattended operation state.\n2. If .gir/CLAUDE-activeContext.md exists, read it to restore session context. If neither MISSION.md nor activeContext.md exists, briefly mention the user can run /gir:init-memory-bank to set up a memory bank.\n3. If .gir/ESCALATION.md exists, read it silently and hold the must-escalate conditions in context — do not summarize to the user unless a condition is immediately relevant.\n4. If .gir/GIR.modules exists, read it and ensure gir-core is listed using this exact format:\n\n### gir-core (v2.2.0) — core\n- **Agents**: feature-architect, code-reviewer, debugger, team-lead, spec-analyst\n- **Skills (always)**: routing-stub, core-practices, state-machines\n- **Skills (on-demand)**: workflows, ralph-loops, specgates\n- **Commands**: init-project, init-memory-bank, drift-check, status, modules, load-workflows, load-specgates, load-ralph, update, init-setup\n- **MCP**: gir-sequential-thinking\n\nDo not remove spoke entries — stale entries from uninstalled spokes are harmless. If .gir/GIR.modules doesn't exist but .gir/ does, create it with just the gir-core entry above. Do all of this silently.\n\n5. If .gir/MISSION.md exists and contains an autonomous_run block with status \"in-progress\": check the checkpoint_at timestamp. If more than 65 minutes have elapsed since checkpoint_at, the session likely ended due to usage cap. If resume_after_cap is true, automatically resume by invoking /gir:run. If resume_after_cap is false, notify the user: \"Autonomous run was interrupted (possible usage cap). Run /gir:run to resume from phase [N].\" Do this silently if unattended is true."
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 5: Update marketplace.json source path and plugin entry name**

In `.claude-plugin/marketplace.json`, update the core plugin entry:
```json
{
  "name": "gir-core",
  "source": "./plugins/gir-core",
  "description": "Core agents, routing-stub orchestration, workflows, and best practices for any Claude Code project",
  "version": "2.2.0",
  "category": "productivity",
  "keywords": ["agents", "workflows", "best-practices", "delegation", "code-review"]
},
```

- [ ] **Step 6: Update requires in all 7 spoke gir-module.json files**

Run this to verify all 7 occurrences before editing:
```bash
grep -r '"requires": \["gir"\]' plugins/ --include="gir-module.json" -l
```
Expected output: 7 files (gir-web, gir-automation, gir-tools, gir-database, gir-ai, gir-qa, gir-migrate)

In each of the 7 files, change:
```json
"requires": ["gir"]
```
to:
```json
"requires": ["gir-core"]
```

Files to edit:
- `plugins/gir-web/gir-module.json`
- `plugins/gir-automation/gir-module.json`
- `plugins/gir-tools/gir-module.json`
- `plugins/gir-database/gir-module.json`
- `plugins/gir-ai/gir-module.json`
- `plugins/gir-qa/gir-module.json`
- `plugins/gir-migrate/gir-module.json`

- [ ] **Step 7: Verify no stale `"gir"` name references remain**

```bash
grep -r '"name": "gir"' plugins/ --include="*.json"
grep -r '"requires": \["gir"\]' plugins/ --include="*.json"
```
Expected: no output from either command.

- [ ] **Step 8: Commit**

```bash
git add plugins/gir-core/ plugins/gir-web/gir-module.json plugins/gir-automation/gir-module.json plugins/gir-tools/gir-module.json plugins/gir-database/gir-module.json plugins/gir-ai/gir-module.json plugins/gir-qa/gir-module.json plugins/gir-migrate/gir-module.json .claude-plugin/marketplace.json
git commit -m "feat: rename core plugin from gir to gir-core"
```

---

## Task 3: Fix Sequential-Thinking MCP Conflict

When gir-core is installed alongside a user's existing `sequential-thinking` MCP server, the duplicate server name causes a conflict. Fix: rename the server key in `.mcp.json` to `gir-sequential-thinking` — this avoids any collision with user-managed installations.

**Files:**
- Modify: `plugins/gir-core/.mcp.json`
- Modify: `plugins/gir-core/skills/routing-stub.md` — find and update MCP server name reference

Note: `gir-module.json` was already updated in Task 2 Step 3.

- [ ] **Step 1: Update .mcp.json server key**

Full file content for `plugins/gir-core/.mcp.json`:
```json
{
  "mcpServers": {
    "gir-sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "type": "stdio"
    }
  }
}
```

- [ ] **Step 2: Find sequential-thinking references in routing-stub**

```bash
grep -n "sequential-thinking" plugins/gir-core/skills/routing-stub.md
```

Update every reference from `sequential-thinking` to `gir-sequential-thinking` in `plugins/gir-core/skills/routing-stub.md`.

- [ ] **Step 3: Verify no stale key remains**

```bash
grep -r '"sequential-thinking"' plugins/gir-core/ --include="*.json"
```
Expected: no output (only `gir-sequential-thinking` should appear).

- [ ] **Step 4: Commit**

```bash
git add plugins/gir-core/.mcp.json plugins/gir-core/gir-module.json plugins/gir-core/skills/routing-stub.md
git commit -m "fix: rename sequential-thinking MCP key to gir-sequential-thinking to avoid conflicts"
```

---

## Task 4: gir-ai — AI Integration Install Flow

When gir-ai installs and no AI integrations are configured, the plugin should detect this and surface a template file so users know how to wire up API keys. Add detection logic to the SessionStart hook and create a template file.

**Files:**
- Modify: `plugins/gir-ai/hooks/hooks.json`
- Create: `plugins/gir-ai/templates/ai-integrations.md`

- [ ] **Step 1: Create the template file**

Create `plugins/gir-ai/templates/ai-integrations.md`:
```markdown
# AI Integrations

Copy this file to `.gir/ai-integrations.md` and fill in the integrations you use.
The `ai-delegation` skill uses this file to know which tools are available for delegation.

## Gemini CLI
- Status: [ ] installed  [ ] not using
- Install: `npm install -g @google/generative-ai-cli` or via Homebrew
- API key env var: `GEMINI_API_KEY`
- Set in shell profile: `export GEMINI_API_KEY=your-key-here`

## OpenAI Codex
- Status: [ ] installed  [ ] not using
- Install: `npm install -g @openai/codex`
- API key env var: `OPENAI_API_KEY`
- Set in shell profile: `export OPENAI_API_KEY=your-key-here`

## Anthropic Claude (API direct)
- Status: [ ] installed  [ ] not using
- API key env var: `ANTHROPIC_API_KEY`
- Set in shell profile: `export ANTHROPIC_API_KEY=your-key-here`

## Other (add yours below)
<!-- Format: Name, install command, env var -->
```

- [ ] **Step 2: Update gir-ai hooks.json**

Replace current `hooks/hooks.json` with install-flow detection logic:

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If .gir/ directory exists in the current project:\n\n1. Check if .gir/GIR.modules exists. If it does, read it and ensure gir-ai is listed. If gir-ai is missing, append the following entry exactly as shown:\n\n### gir-ai (v2.0.0) — spoke\n- **Agents**: (none)\n- **Skills**: ai-delegation\n- **Commands**: (none)\n- **MCP**: (none)\n\nWrite the updated file. Do this silently.\n\n2. Check if .gir/ai-integrations.md exists. If it does NOT exist, tell the user: \"gir-ai is installed but no AI integrations are configured. Copy plugins/gir-ai/templates/ai-integrations.md to .gir/ai-integrations.md and fill in the tools you use — or ask which integrations you'd like to set up and I'll help configure them.\""
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add plugins/gir-ai/hooks/hooks.json plugins/gir-ai/templates/ai-integrations.md
git commit -m "feat(gir-ai): add AI integration detection and template on first install"
```

---

## Task 5: Rename `gir-tools` → `gir-atc`

`gir-atc` stands for Agentic Traffic Control. The subtask-manager and agenthub agents are removed (subtask-manager was deprecated in v2.2.0; agenthub requires an external macOS app with no replacement in-repo). The plugin retains its identity as the home for advanced multi-agent coordination patterns and becomes the foundation for future ATC tooling.

**Files:**
- Rename dir: `plugins/gir-tools/` → `plugins/gir-atc/`
- Modify: `plugins/gir-atc/.claude-plugin/plugin.json`
- Modify: `plugins/gir-atc/gir-module.json`
- Modify: `plugins/gir-atc/hooks/hooks.json`
- Modify: `plugins/gir-atc/README.md`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `README.md` (root)

- [ ] **Step 1: Rename directory**

```bash
git mv plugins/gir-tools plugins/gir-atc
```

- [ ] **Step 2: Update plugin.json**

Full file content for `plugins/gir-atc/.claude-plugin/plugin.json`:
```json
{
  "name": "gir-atc",
  "version": "2.0.0",
  "description": "Requires gir-core. Agentic traffic control — multi-agent dispatch, coordination patterns, and session routing",
  "author": { "name": "rivit-studio", "url": "https://github.com/rivit-studio" },
  "homepage": "https://github.com/rivit-studio/GIR",
  "repository": "https://github.com/rivit-studio/GIR",
  "license": "MIT",
  "keywords": ["agentic", "traffic-control", "coordination", "dispatch", "multi-agent"]
}
```

- [ ] **Step 3: Update gir-module.json**

Full file content for `plugins/gir-atc/gir-module.json`:
```json
{
  "name": "gir-atc",
  "version": "2.0.0",
  "type": "spoke",
  "description": "Agentic traffic control — multi-agent dispatch and coordination patterns",
  "requires": ["gir-core"],
  "provides": {
    "agents": [],
    "skills": [],
    "commands": [],
    "mcp": {}
  }
}
```

- [ ] **Step 4: Update hooks.json**

Full file content for `plugins/gir-atc/hooks/hooks.json`:
```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If .gir/ directory exists in the current project, check if .gir/GIR.modules exists. If it does, read it and ensure gir-atc is listed. If gir-atc is missing, append the following entry exactly as shown:\n\n### gir-atc (v2.0.0) — spoke\n- **Agents**: (none)\n- **Skills**: (none)\n- **Commands**: (none)\n- **MCP**: (none)\n\nWrite the updated file. Do this silently without telling the user."
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 5: Update README.md in plugin directory**

Read current `plugins/gir-atc/README.md`, then rewrite the header and description section:
- Change plugin name heading to `gir-atc — Agentic Traffic Control`
- Remove all references to subtask-manager, agenthub, subtask CLI, AgentHub macOS app
- Update description: "gir-atc is the agentic traffic control spoke for GIR. It is the designated home for multi-agent dispatch patterns, session routing logic, and coordination tooling. The native `parallel-orchestrator` agent in `gir-core` handles parallel workstreams; this module expands on advanced coordination patterns."
- Keep requires, install, and uninstall sections accurate

- [ ] **Step 6: Update marketplace.json gir-tools entry**

In `.claude-plugin/marketplace.json`, replace the `gir-tools` plugin entry with:
```json
{
  "name": "gir-atc",
  "source": "./plugins/gir-atc",
  "description": "Agentic traffic control — multi-agent dispatch, coordination patterns, and session routing",
  "version": "2.0.0",
  "category": "productivity",
  "keywords": ["agentic", "traffic-control", "coordination", "dispatch", "multi-agent"]
},
```

- [ ] **Step 7: Update root README.md**

Find all `gir-tools` references in root `README.md`:
```bash
grep -n "gir-tools" README.md
```

Replace each occurrence:
- Module table row: change `gir-tools` to `gir-atc`, update description to "Agentic traffic control — multi-agent dispatch and coordination patterns"
- Architecture mermaid diagram: change `gir-tools` node label to `gir-atc`
- Install command: change `claude plugin install gir-tools` to `claude plugin install gir-atc`
- Any other prose references: update accordingly

- [ ] **Step 8: Verify no stale gir-tools references**

```bash
grep -r "gir-tools" . --exclude-dir=".git" --exclude-dir="graphify-out" -l
```
Expected: no output.

- [ ] **Step 9: Commit**

```bash
git add plugins/gir-atc/ .claude-plugin/marketplace.json README.md
git commit -m "feat: rename gir-tools to gir-atc (agentic traffic control), remove deprecated agents"
```

---

## Task 6: Version Bump to v2.5.1 + GitHub Release

All plugins normalize to `v2.5.1`. The marketplace collection metadata also bumps. `gir-migrate` stays at its own version track (`v1.0.0`) since it is a one-time utility — bump it to `v1.1.0` to reflect the rename-compatible update it received in this overhaul. Then create a GitHub release.

**Files (25 minimum):**
- `plugins/gir-core/.claude-plugin/plugin.json` — `2.2.0` → `2.5.1`
- `plugins/gir-core/gir-module.json` — `2.2.0` → `2.5.1`
- `plugins/gir-core/hooks/hooks.json` — `v2.2.0` in prompt text → `v2.5.1`
- `plugins/gir-web/.claude-plugin/plugin.json` — `2.0.0` → `2.5.1`
- `plugins/gir-web/gir-module.json` — `2.0.0` → `2.5.1`
- `plugins/gir-web/hooks/hooks.json` — `v2.0.0` in prompt text → `v2.5.1`
- `plugins/gir-automation/.claude-plugin/plugin.json` — `2.5.1`
- `plugins/gir-automation/gir-module.json` — `2.5.1`
- `plugins/gir-automation/hooks/hooks.json` — `v2.5.1`
- `plugins/gir-atc/.claude-plugin/plugin.json` — `2.5.1`
- `plugins/gir-atc/gir-module.json` — `2.5.1`
- `plugins/gir-atc/hooks/hooks.json` — `v2.5.1`
- `plugins/gir-database/.claude-plugin/plugin.json` — `2.5.1`
- `plugins/gir-database/gir-module.json` — `2.5.1`
- `plugins/gir-database/hooks/hooks.json` — `v2.5.1`
- `plugins/gir-ai/.claude-plugin/plugin.json` — `2.5.1`
- `plugins/gir-ai/gir-module.json` — `2.5.1`
- `plugins/gir-ai/hooks/hooks.json` — `v2.5.1`
- `plugins/gir-qa/.claude-plugin/plugin.json` — `2.5.1`
- `plugins/gir-qa/gir-module.json` — `2.5.1`
- `plugins/gir-qa/hooks/hooks.json` — `v2.5.1`
- `plugins/gir-migrate/.claude-plugin/plugin.json` — `1.0.0` → `1.1.0`
- `plugins/gir-migrate/gir-module.json` — `1.0.0` → `1.1.0`
- `plugins/gir-migrate/hooks/hooks.json` — `v1.0.0` → `v1.1.0`
- `.claude-plugin/marketplace.json` — `metadata.version` `2.0.0` → `2.5.1`, all plugin version fields
- `README.md` — badge `version-2.2.0-blue` → `version-2.5.1-blue`

- [ ] **Step 1: Verify all current version strings**

```bash
grep -rn '"version"' plugins/ --include="plugin.json" --include="gir-module.json"
grep -n "version" .claude-plugin/marketplace.json
grep -n "version-" README.md
```

- [ ] **Step 2: Update all plugin.json and gir-module.json version fields**

For each plugin (gir-core, gir-web, gir-automation, gir-atc, gir-database, gir-ai, gir-qa), edit `plugin.json` and `gir-module.json`: change `"version": "2.x.x"` to `"version": "2.5.1"`.

For gir-migrate, change `"version": "1.0.0"` to `"version": "1.1.0"` in both files.

- [ ] **Step 3: Update hooks.json version strings in prompt text**

Each hooks.json embeds the version number inside the prompt string (e.g., `### gir-core (v2.2.0) — core`). These are inside JSON string values, not version fields, so they require careful per-file edits.

For each plugin hooks.json, find the `### plugin-name (vX.X.X)` line in the prompt string and update the version:

- `plugins/gir-core/hooks/hooks.json`: `gir-core (v2.2.0)` → `gir-core (v2.5.1)`
- `plugins/gir-web/hooks/hooks.json`: `gir-web (v2.0.0)` → `gir-web (v2.5.1)`
- `plugins/gir-automation/hooks/hooks.json`: `gir-automation (v2.0.0)` → `gir-automation (v2.5.1)`
- `plugins/gir-atc/hooks/hooks.json`: `gir-atc (v2.0.0)` → `gir-atc (v2.5.1)`
- `plugins/gir-database/hooks/hooks.json`: `gir-database (v2.0.0)` → `gir-database (v2.5.1)`
- `plugins/gir-ai/hooks/hooks.json`: `gir-ai (v2.0.0)` → `gir-ai (v2.5.1)`
- `plugins/gir-qa/hooks/hooks.json`: `gir-qa (v2.0.0)` → `gir-qa (v2.5.1)`
- `plugins/gir-migrate/hooks/hooks.json`: `gir-migrate (v1.0.0)` → `gir-migrate (v1.1.0)`

- [ ] **Step 4: Update marketplace.json version fields**

In `.claude-plugin/marketplace.json`:
- Change `"metadata": { "version": "2.0.0" }` → `"version": "2.5.1"`
- Change every plugin entry `"version"` field to `"2.5.1"` (all 7 main plugins)
- Change gir-migrate entry `"version"` to `"1.1.0"`

- [ ] **Step 5: Update README badge**

In `README.md`, find:
```
https://img.shields.io/badge/version-2.2.0-blue
```
Replace with:
```
https://img.shields.io/badge/version-2.5.1-blue
```

- [ ] **Step 6: Verify all version strings**

```bash
grep -rn '"version"' plugins/ --include="plugin.json" --include="gir-module.json"
```
Expected: all show `2.5.1` except gir-migrate which shows `1.1.0`.

```bash
grep -rn "v2\." plugins/ --include="hooks.json"
```
Expected: all show `v2.5.1`.

```bash
grep -n "version" .claude-plugin/marketplace.json
```
Expected: metadata shows `2.5.1`, all plugin entries show `2.5.1`, gir-migrate shows `1.1.0`.

- [ ] **Step 7: Commit version bump**

```bash
git add plugins/ .claude-plugin/marketplace.json README.md
git commit -m "chore: bump all plugins to v2.5.1 (gir-migrate to v1.1.0)"
```

- [ ] **Step 8: Merge dev → prod (requires human confirmation)**

> **Warning:** This pushes to the `prod` branch. Per project policy, do NOT merge without explicit YES from the user.

Ask the user: "All v2.5.1 changes committed on dev. Ready to merge dev → prod and cut the GitHub release? Type YES to proceed."

After YES:
```bash
git checkout prod
git merge dev
git push origin prod
```

- [ ] **Step 9: Create GitHub release**

```bash
gh release create v2.5.1 \
  --title "GIR v2.5.1 — Ecosystem Overhaul" \
  --notes "$(cat <<'EOF'
## What's Changed

### Breaking: Core Plugin Renamed
- `gir` → `gir-core` — reinstall with `claude plugin install gir-core`
- All spoke plugins updated to require `gir-core`

### Breaking: gir-tools Renamed
- `gir-tools` → `gir-atc` (Agentic Traffic Control)
- Deprecated `subtask-manager` and `agenthub` agents removed
- Reinstall with `claude plugin install gir-atc`

### Fixes
- Sequential-thinking MCP server renamed to `gir-sequential-thinking` — no longer conflicts with user-installed `sequential-thinking` servers
- Marketplace collection display name fixed: now shows as `gir` in Claude Code TUI

### New
- `gir-ai`: First-run integration detection — prompts to configure AI tools if none are set up
- `gir-ai/templates/ai-integrations.md`: Template for wiring Gemini CLI, Codex, Anthropic API

### Versioning
- All spoke plugins: `2.0.0` → `2.5.1`
- Core (`gir-core`): `2.2.0` → `2.5.1`
- `gir-migrate`: `1.0.0` → `1.1.0`
EOF
)"
```

---

## Self-Review

**Spec coverage check:**
- ✅ Marketplace/TUI name fix (`gir-plugins` → `gir`) — Task 1
- ✅ Core module renamed to `gir-core` — Task 2
- ✅ sequential-thinking conflict fix — Task 3
- ✅ gir-ai install flow with template — Task 4
- ✅ gir-tools → gir-atc with description update — Task 5
- ✅ Version bump to v2.5.1 + GitHub release — Task 6

**Dependency order check:**
- Task 2 (gir-core rename) subsumes the MCP key rename in gir-module.json to avoid a second touch — flagged in Task 3 Step 1
- Task 5 Step 3 sets `requires: ["gir-core"]` not `["gir"]` — correct post-Task-2
- Task 6 picks up `gir-atc` not `gir-tools` — correct post-Task-5
- Task 6 Step 8 (merge to prod) has mandatory human confirmation gate per project policy

**Risk flags:**
- hooks.json prompt strings embed version numbers in natural language — cannot use a simple regex replace; each file needs a targeted edit (covered in Task 6 Step 3)
- `gir-migrate` intentionally bumps to `1.1.0` not `2.5.1` — different version track
